naftiko: 1.0.0-alpha2
info:
  label: Core API — Velocity Controls Card Group Balance
  description: 'Core API — Velocity Controls Card Group Balance. 1 operations. Lead operation: List Velocity Controls Card
    Group Balances. Self-contained Naftiko capability covering one Marqeta business surface.'
  tags:
  - Marqeta
  - Velocity Controls Card Group Balance
  created: '2026-05-19'
  modified: '2026-05-19'
binds:
- namespace: env
  keys:
    MARQETA_API_KEY: MARQETA_API_KEY
capability:
  consumes:
  - type: http
    namespace: core-velocity-controls-card-group-balance
    baseUri: ''
    description: Core API — Velocity Controls Card Group Balance business capability. Self-contained, no shared references.
    resources:
    - name: velocitycontrols-cardgroup-card_group_token-available
      path: /velocitycontrols/cardgroup/{card_group_token}/available
      operations:
      - name: listvelocitycontrolscardgroupbalances
        method: GET
        description: List Velocity Controls Card Group Balances
        outputRawFormat: json
        outputParameters:
        - name: result
          type: object
          value: $.
        inputParameters:
        - name: card_group_token
          in: path
          type: string
          description: Unique identifier of the card group for which to retrieve balances.
          required: true
    authentication:
      type: basic
      username: '{{env.MARQETA_USER}}'
      password: '{{env.MARQETA_PASS}}'
  exposes:
  - type: rest
    namespace: core-velocity-controls-card-group-balance-rest
    port: 8080
    description: REST adapter for Core API — Velocity Controls Card Group Balance. One Spectral-compliant resource per consumed
      operation, prefixed with /v1.
    resources:
    - path: /v1/velocitycontrols/cardgroup/{card-group-token}/available
      name: velocitycontrols-cardgroup-card-group-token-available
      description: REST surface for velocitycontrols-cardgroup-card_group_token-available.
      operations:
      - method: GET
        name: listvelocitycontrolscardgroupbalances
        description: List Velocity Controls Card Group Balances
        call: core-velocity-controls-card-group-balance.listvelocitycontrolscardgroupbalances
        with:
          card_group_token: rest.card_group_token
        outputParameters:
        - type: object
          mapping: $.
  - type: mcp
    namespace: core-velocity-controls-card-group-balance-mcp
    port: 9090
    transport: http
    description: MCP adapter for Core API — Velocity Controls Card Group Balance. One tool per consumed operation, routed
      inline through this capability's consumes block.
    tools:
    - name: list-velocity-controls-card-group
      description: List Velocity Controls Card Group Balances
      hints:
        readOnly: true
        destructive: false
        idempotent: true
      call: core-velocity-controls-card-group-balance.listvelocitycontrolscardgroupbalances
      with:
        card_group_token: tools.card_group_token
      outputParameters:
      - type: object
        mapping: $.
